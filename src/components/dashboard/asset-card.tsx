import { StatusBadge, type AssetStatus } from "@/components/dashboard/status-badge";
export interface DashboardAsset { id: string; name: string; category: string; image: string; status: AssetStatus; }
export function AssetCard({ asset }: { asset: DashboardAsset }) { return <article className="group overflow-hidden rounded-2xl border border-white/10 bg-[#111] transition duration-300 hover:-translate-y-1 hover:border-white/25"><div className="relative aspect-[1.45] overflow-hidden"><img src={asset.image} alt={asset.name} className="size-full object-cover grayscale transition duration-500 group-hover:scale-105 group-hover:grayscale-0" /><span className="absolute left-4 top-4 rounded bg-black/50 px-2 py-1 font-mono text-[9px] uppercase text-slate-200 backdrop-blur">Vehicle Asset / Battery: 82%</span><div className="absolute right-4 top-4"><StatusBadge status={asset.status} /></div></div><div className="border-t border-white/10 p-5"><p className="font-mono text-xs font-semibold uppercase tracking-wide text-slate-400">Unit ID: {asset.id}</p><h3 className="mt-2 text-lg font-semibold text-white">{asset.name}</h3><p className="mt-1 text-sm text-slate-400">{asset.category}</p></div></article>; }
